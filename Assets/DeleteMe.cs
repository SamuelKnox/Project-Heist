using UnityEngine;
using System.Collections;

public class DeleteMe : MonoBehaviour {

	// Use this for initialization
	void Start () {
        transform.RotateOverTime(new Vector3(0, 90, 0), 1.5F);

	}
	 void FixedUpdate()
 {
 }
	}
